[project]
name = "cvec-db"
version = "0.1.0"
description = "CVE database builder - generates parquet files for cvec tool"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
keywords = ["cve", "security", "vulnerability", "database"]
dependencies = [
    "cvec @ git+https://github.com/RomainRiv/cvec.git@4f73ed07016d1070f088fb88f33ff7d81b19a625",
    "typer>=0.16.0",
    "polars>=1.31.0",
]

[project.scripts]
cvec-db = "cvec_db.cli:main"

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cvec_db"]
